meta {
  name: Patch Completed
  type: http
  seq: 4
}

patch {
  url: http://localhost:3000/api/tasks/1/completed
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("should toggle task completion status", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property('message');
    expect(res.body).to.have.property('task');
    expect(res.body.task).to.have.property('state');
  });
}
